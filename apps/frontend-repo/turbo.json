{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "env": ["PORT", "NODE_ENV", "FIRESTORE_EMULATOR_HOST", "FIREBASE_AUTH_EMULATOR_HOST"],
      "outputs": [
        ".next/**",
        "!.next/cache/**"
      ]
    }
  }
}
