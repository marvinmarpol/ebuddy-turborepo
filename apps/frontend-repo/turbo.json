{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "env": ["PORT", "NODE_ENV", "FIRESTORE_EMULATOR_HOST", "AUTH_EMULATOR_HOST"],
      "outputs": [
        ".next/**",
        "!.next/cache/**"
      ]
    }
  }
}
