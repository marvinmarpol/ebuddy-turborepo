{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["PORT", "NODE_ENV", "FIREBASE_ACCOUNT_PATH", "FIRESTORE_EMULATOR_HOST", "FIREBASE_AUTH_EMULATOR_HOST"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    }
  }
}
